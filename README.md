# Events Frontend

Фронтенд-приложение для поиска и управления мероприятиями.

## 🚀 Технологии

- **React 18** + **TypeScript**
- **Vite** - быстрая сборка
- **React Router v6** - маршрутизация
- **Axios** - HTTP-клиент
- **Zustand** - управление состоянием
- **Tailwind CSS** - стилизация
- **react-icons** - иконки
- **date-fns** - работа с датами
- **react-hook-form** - формы
- **react-hot-toast** - уведомления
- **react-datepicker** - выбор дат

## 📁 Структура проекта (Feature-Sliced Design)

```
src/
├── app/                # Глобальная конфигурация
│   ├── store/          # Zustand stores
│   ├── providers/      # Провайдеры (Router)
│   └── App.tsx
├── pages/              # Страницы приложения
│   ├── LoginPage/
│   ├── RegisterPage/
│   ├── EventsPage/
│   ├── EventDetailPage/
│   ├── ProfilePage/
│   └── NotFoundPage/
├── widgets/            # Крупные переиспользуемые блоки
│   └── Header/
├── features/           # Функциональные модули
│   └── EventFilters/
├── entities/           # Бизнес-сущности
│   └── Event/
└── shared/             # Общий код
    ├── api/            # API сервисы
    ├── hooks/          # Кастомные хуки
    ├── lib/            # Утилиты
    └── types/          # TypeScript типы
```

## 🛠️ Установка и запуск

1. Установите зависимости:
```bash
npm install
```

2. Создайте файл `.env` на основе `.env.example`:
```bash
VITE_API_BASE_URL=http://localhost:8000/api
```

3. Запустите dev-сервер:
```bash
npm run dev
```

4. Соберите для production:
```bash
npm run build
```

## 📱 Маршруты

| Путь | Страница | Защита |
|------|----------|--------|
| `/` | Список мероприятий | Guest / Auth |
| `/event/:id` | Детали мероприятия | Guest / Auth |
| `/login` | Вход | Guest only |
| `/register` | Регистрация | Guest only |
| `/profile` | Профиль | Auth only |
| `*` | 404 | — |

## 🔐 Авторизация

Приложение использует JWT-токены, которые хранятся в localStorage. При 401 ошибке происходит автоматический редирект на страницу входа.

## 🎨 Особенности

- **Оптимистичные обновления UI** - мгновенная реакция на действия пользователя
- **Синхронизация фильтров с URL** - возможность делиться ссылками с примененными фильтрами
- **Адаптивный дизайн** - работает на всех устройствах
- **TypeScript** - полная типизация
- **Feature-Sliced Design** - архитектура для масштабируемости

## 📦 API

Все API-вызовы находятся в `src/shared/api/`:
- `authApi` - авторизация и регистрация
- `userApi` - работа с пользователем
- `eventsApi` - работа с мероприятиями
