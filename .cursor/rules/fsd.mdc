---
description: 
globs: 
alwaysApply: true
---
# 🧱 Feature-Sliced Design (FSD)

## 📁 Структура фичи

```
src/
├── pages/               # Полные страницы
│   └── HomePage/
│       ├── index.tsx     # Точка входа страницы
│       └── components/   # Локальные компоненты страницы
├── widgets/              # Крупные переиспользуемые блоки
│   └── Header/
│       ├── index.tsx
│       └── model/
├── features/             # Фичи приложения
│   └── AuthByUsername/
│       ├── ui/            # UI-компоненты
│       │   ├── LoginForm/
│       │   └── RegisterForm/
│       └── model/         # Бизнес-логика фичи
│           ├── api/
│           └── types/
├── entities/             # Бизнес-объекты
│   └── User/
│       ├── model/
│       └── types/
├── shared/               # Общие утилиты и библиотека
    └── lib/
        ├── helpers/
        └── hooks/
```

---

## 🧩 Принципы FSD

### 1. **Слои**
- `app` — глобальная конфигурация (роутинг, темизация, провайдеры)
- `pages` — полные страницы приложения
- `widgets` — крупные переиспользуемые компоненты (шапка, футер, сайдбар)
- `features` — функциональные модули (логин, добавление товара, лайк и т.д.)
- `entities` — бизнес-сущности (User, Product)
- `shared` — общий код, не зависящий от домена

---

## 🧱 Формат файла фичи (`features/.../ui/Component/index.tsx`)

```tsx
import React from 'react';

interface ComponentProps {
  className?: string;
}

export const Component: React.FC<ComponentProps> = ({ className }) => {
  return (
    <div className={className}>
      <!-- Реализация фичи -->
    </div>
  );
};
```

---

## 📦 Экспорт через `index.ts`

Каждая папка должна иметь файл `index.ts`, который экспортирует содержимое по дефолту или именованно:

```ts
// src/features/AuthByUsername/ui/LoginForm/index.ts
export { LoginForm } from './LoginForm';
```

---

## 🔗 Импорт через псевдонимы

Используй алиасы в `tsconfig.json`:

```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@app/*": ["src/app/*"],
      "@pages/*": ["src/pages/*"],
      "@widgets/*": ["src/widgets/*"],
      "@features/*": ["src/features/*"],
      "@entities/*": ["src/entities/*"],
      "@shared/*": ["src/shared/*"]
    }
  }
}
```

Пример импорта:
```ts
import { LoginForm } from '@features/AuthByUsername/ui/LoginForm';
```

---

## ✅ Чек-лист для новой фичи

### 1. **Файловая структура**
- [ ] Фича находится в `/features/<FeatureName>/`
- [ ] Есть подкаталоги `ui/` и `model/`
- [ ] UI-компоненты выделены в отдельные файлы
- [ ] Все экспортные пути описаны через `index.ts`

### 2. **UI**
- [ ] Компоненты имеют типизацию через `interface`
- [ ] Используется `React.FC<Props>`
- [ ] `className` передается как пропс

### 3. **Модель**
- [ ] Логика фичи отделена от UI
- [ ] API-вызовы вынесены в `model/api/`
- [ ] Типы вынесены в `model/types/`

### 4. **Тесты**
- [ ] Написаны unit-тесты для модели
- [ ] Snapshot-тесты для UI
- [ ] Использованы моки при тестировании API

---

## 🚫 Антипаттерны

1. **Громадный компонент** — всё в одном файле
2. **Смешение слоёв** — UI вызывает напрямую API
3. **Дублирование типов** — одинаковые интерфейсы в разных местах
4. **Отсутствие разделения по слоям** — например, бизнес-логика в `pages`
5. **"Хрустальный шар" в props** — когда компонент принимает слишком много неявных данных
